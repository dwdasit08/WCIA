version: '3.8'
services:
  wcia:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - EMAIL_USER=${EMAIL_USER}
      - EMAIL_PASS=${EMAIL_PASS}
    env_file:
      - .env
    restart: unless-stopped